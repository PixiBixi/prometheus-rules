- name: prometheus.rules
  rules:
    - alert: PushGatewayDown
      expr: |
        # Two distinct failure modes: a failing scrape (up == 0, exporter crashed
        # or host unreachable) and the target vanishing from service discovery,
        # where no up series exists to be 0. Both branches are grouped
        # identically so the alert carries the same labels either way.
        group by (job, instance) (up{job="prometheus-pushgateway"} == 0)
        or
        (
          group by (job, instance) (last_over_time(up{job="prometheus-pushgateway"}[1h]))
          unless
          group by (job, instance) (last_over_time(up{job="prometheus-pushgateway"}[2m]))
        )
      for: 2m
      labels:
        severity: critical
      annotations:
        description: "{{ $labels.instance }} of job {{ $labels.job }} has been down for more than 2 minutes."
        summary: "Job {{ $labels.job }} having an issue on {{ $labels.instance }}"
    - alert: PrometheusConfigReloadFailed
      expr: prometheus_config_last_reload_successful == 0
      for: 10m
      labels:
        severity: warning
      annotations:
        summary: reload of Prometheus configuration has failed for {{ $labels.instance }}
    - alert: PrometheusErrorSendingAlerts
      expr: rate(prometheus_notifications_errors_total[5m]) / rate(prometheus_notifications_sent_total[5m]) > 0.03
      for: 10m
      labels:
        severity: warning
      annotations:
        summary: "Errors while sending alerts from Prometheus {{ $labels.instance }} to Alertmanager {{ $labels.alertmanager }}"
    - alert: PrometheusJobMissing
      expr: absent(up{job="prometheus"})
      labels:
        severity: warning
      annotations:
        summary: "Prometheus job missing (job {{ $labels.job }})"
        description: "A Prometheus job has disappeared"
    - alert: PrometheusTargetMissing
      # Scoped to the monitoring stack itself. This used to be unscoped and
      # acted as the only real `up == 0` net for every exporter in the repo,
      # since the per-exporter watchdogs only detected targets vanishing from
      # service discovery. Those now cover both cases under their own names,
      # so an unscoped net here would just double-notify without a runbook.
      expr: up{job=~"prometheus|alertmanager|.*pushgateway"} == 0 unless on(job) (sum by (job) (up) == 0)
      for: 1m
      labels:
        severity: critical
      annotations:
        summary: "Prometheus target missing (instance {{ $labels.instance }})"
        description: "A Prometheus target has disappeared. An exporter might be crashed."
    - alert: PrometheusAllTargetsMissing
      expr: sum by (job) (up) == 0
      for: 1m
      labels:
        severity: critical
      annotations:
        summary: "Prometheus all targets missing (job {{ $labels.job }})"
        description: "A Prometheus job does not have living target anymore."
    - alert: PrometheusAlertManagerJobMissing
      expr: absent(up{job="alertmanager"})
      labels:
        severity: warning
      annotations:
        summary: "Prometheus AlertManager job missing (job {{ $labels.job }})"
        description: "A Prometheus AlertManager job has disappeared"
    - alert: PrometheusTooManyRestarts
      expr: changes(process_start_time_seconds{job=~"prometheus|.*pushgateway|alertmanager"}[15m]) > 2
      labels:
        severity: warning
      annotations:
        summary: "Prometheus too many restarts (instance {{ $labels.instance }})"
        description: "Prometheus has restarted more than twice in the last 15 minutes. It might be crashlooping."
    - alert: PrometheusAlertmanagerConfigurationReloadFailure
      expr: alertmanager_config_last_reload_successful != 1
      labels:
        severity: warning
      annotations:
        summary: "Prometheus AlertManager configuration reload failure (instance {{ $labels.instance }})"
        description: "AlertManager configuration reload error"
    - alert: PrometheusAlertmanagerConfigNotSynced
      # Aggregated per job: two independent Alertmanager clusters legitimately
      # run different configs. The for: rides out the few seconds of divergence
      # during a rolling config update.
      expr: count by (job) (count_values by (job) ("config_hash", alertmanager_config_hash)) > 1
      for: 15m
      labels:
        severity: warning
      annotations:
        summary: "Prometheus AlertManager config not synced (instance {{ $labels.instance }})"
        description: "Configurations of AlertManager cluster instances are out of sync"
    - alert: PrometheusNotConnectedToAlertmanager
      # Transient during an Alertmanager service-discovery refresh.
      expr: prometheus_notifications_alertmanagers_discovered < 1
      for: 5m
      labels:
        severity: critical
      annotations:
        summary: "Prometheus not connected to alertmanager (instance {{ $labels.instance }})"
        description: "Prometheus cannot connect the alertmanager"
    - alert: PrometheusRuleEvaluationFailures
      expr: increase(prometheus_rule_evaluation_failures_total[3m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus rule evaluation failures (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} rule evaluation failures, leading to potentially ignored alerts."
    - alert: PrometheusTemplateTextExpansionFailures
      expr: increase(prometheus_template_text_expansion_failures_total[3m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus template text expansion failures (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} template text expansion failures"
    - alert: PrometheusRuleEvaluationSlow
      expr: prometheus_rule_group_last_duration_seconds > prometheus_rule_group_interval_seconds
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Prometheus rule evaluation slow (instance {{ $labels.instance }})"
        description: "Prometheus rule evaluation took more time than the scheduled interval. It indicates a slower storage backend access or too complex query."
    - alert: PrometheusNotificationsBacklog
      expr: min_over_time(prometheus_notifications_queue_length[10m]) > 0
      labels:
        severity: warning
      annotations:
        summary: "Prometheus notifications backlog (instance {{ $labels.instance }})"
        description: "The Prometheus notification queue has not been empty for 10 minutes"
    - alert: PrometheusAlertmanagerNotificationFailing
      expr: rate(alertmanager_notifications_failed_total[3m]) > 0.05
      labels:
        severity: critical
      annotations:
        summary: "Prometheus AlertManager notification failing (instance {{ $labels.instance }})"
        description: "Alertmanager is failing sending notifications ({{ $value | humanize }} failures/s)"
    - alert: PrometheusTargetEmpty
      # Per-SD-config gauge, not global: a podMonitor over a namespace with
      # nothing deployed yet legitimately discovers zero targets.
      expr: prometheus_sd_discovered_targets == 0
      for: 30m
      labels:
        severity: warning
      annotations:
        summary: "Prometheus target empty (instance {{ $labels.instance }}, config {{ $labels.config }})"
        description: "SD config {{ $labels.config }} on {{ $labels.instance }} has discovered no target for 30 minutes"
    - alert: PrometheusTargetScrapingSlow
      # This summary only exposes objectives 0.01 / 0.05 / 0.5 -- quantile="0.9"
      # matched nothing and the alert was dead. Compare the median against the
      # 1st percentile instead.
      expr: prometheus_target_interval_length_seconds{quantile="0.5"} / on (interval, instance, job) prometheus_target_interval_length_seconds{quantile="0.01"} > 1.05
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Prometheus target scraping slow (instance {{ $labels.instance }})"
        description: "Prometheus is scraping exporters slowly since it exceeded the requested interval time. Your Prometheus server is under-provisioned."
    - alert: PrometheusLargeScrape
      expr: increase(prometheus_target_scrapes_exceeded_sample_limit_total[10m]) > 10
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Prometheus large scrape (instance {{ $labels.instance }})"
        description: "Prometheus has many scrapes that exceed the sample limit"
    - alert: PrometheusTargetScrapeDuplicate
      expr: increase(prometheus_target_scrapes_sample_duplicate_timestamp_total[5m]) > 3
      labels:
        severity: warning
      annotations:
        summary: "Prometheus target scrape duplicate (instance {{ $labels.instance }})"
        description: "Prometheus has many samples rejected due to duplicate timestamps but different values ({{ $value }} samples)"
    - alert: PrometheusTsdbCheckpointCreationFailures
      expr: increase(prometheus_tsdb_checkpoint_creations_failed_total[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus TSDB checkpoint creation failures (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} checkpoint creation failures"
    - alert: PrometheusTsdbCheckpointDeletionFailures
      expr: increase(prometheus_tsdb_checkpoint_deletions_failed_total[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus TSDB checkpoint deletion failures (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} checkpoint deletion failures"
    - alert: PrometheusTsdbCompactionsFailed
      expr: increase(prometheus_tsdb_compactions_failed_total[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus TSDB compactions failed (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} TSDB compactions failures"
    - alert: PrometheusTsdbHeadTruncationsFailed
      expr: increase(prometheus_tsdb_head_truncations_failed_total[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus TSDB head truncations failed (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} TSDB head truncation failures"
    - alert: PrometheusTsdbReloadFailures
      expr: increase(prometheus_tsdb_reloads_failures_total[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus TSDB reload failures (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} TSDB reload failures"
    - alert: PrometheusTsdbWalCorruptions
      expr: increase(prometheus_tsdb_wal_corruptions_total[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus TSDB WAL corruptions (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} TSDB WAL corruptions"
    - alert: PrometheusTsdbWalTruncationsFailed
      expr: increase(prometheus_tsdb_wal_truncations_failed_total[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus TSDB WAL truncations failed (instance {{ $labels.instance }})"
        description: "Prometheus encountered {{ $value }} TSDB WAL truncation failures"
    - alert: PrometheusTimeseriesCardinality
      # Two conditions, because neither alone is portable: an absolute floor so
      # a small TSDB stays quiet, and a share of the total so a metric is only
      # flagged when it actually dominates. 10k series is unremarkable next to
      # kube-state-metrics or cadvisor, which is why the bare > 10000 was noisy.
      # $labels.instance is not available here -- the record aggregates it away.
      expr: |
        name:tsdb_series:count > 10000
        and
        name:tsdb_series:count / scalar(tsdb_series:total) > 0.10
      labels:
        severity: warning
      annotations:
        summary: "Prometheus timeseries cardinality — {{ $labels.name }}"
        description: "The \"{{ $labels.name }}\" metric accounts for {{ $value }} series, over 10% of the whole TSDB"
    - alert: PrometheusRemoteWriteDroppedSamples
      # reason="dropped_series" is intentional filtering via write_relabel_configs,
      # not an incident -- excluding it keeps this alert about genuine sample loss.
      #
      # Do not be alarmed if this metric is missing from a healthy instance. It
      # is a CounterVec labelled by `reason` with no child series pre-initialised,
      # so it does not exist until a sample is actually dropped -- which is
      # exactly when this alert should start evaluating. Absence is the healthy
      # state, not a broken rule. Works identically on 2.x and 3.x.
      expr: rate(prometheus_remote_storage_samples_dropped_total{reason!="dropped_series"}[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus remote write dropping samples (instance {{ $labels.instance }})"
        description: "Prometheus is dropping samples for remote_name={{ $labels.remote_name }}, reason={{ $labels.reason }} ({{ $value | humanize }} samples/s lost)"
    - alert: PrometheusRemoteWriteFailed
      expr: rate(prometheus_remote_storage_samples_failed_total[5m]) > 0
      labels:
        severity: critical
      annotations:
        summary: "Prometheus remote write failing (instance {{ $labels.instance }})"
        description: "Prometheus failed to send samples to remote_name={{ $labels.remote_name }} ({{ $value | humanize }} failures/s)"
    - alert: PrometheusRemoteWriteShardsMax
      expr: prometheus_remote_storage_shards >= prometheus_remote_storage_shards_max
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Prometheus remote write shards at max (instance {{ $labels.instance }})"
        description: "Prometheus remote write queue for remote_name={{ $labels.remote_name }} is running at max shards ({{ $value }}). The queue is saturated and samples may be dropped."
    - alert: PrometheusQueryEngineBusy
      expr: prometheus_engine_queries >= prometheus_engine_queries_concurrent_max
      for: 1m
      labels:
        severity: warning
      annotations:
        summary: "Prometheus query engine at concurrency limit (instance {{ $labels.instance }})"
        description: "Prometheus is running {{ $value }} concurrent queries, which equals the maximum allowed. New queries may be rejected."
    - alert: PrometheusTsdbOutOfOrderSamples
      expr: rate(prometheus_tsdb_out_of_order_samples_total[5m]) > 0
      labels:
        severity: warning
      annotations:
        summary: "Prometheus TSDB out-of-order samples (instance {{ $labels.instance }})"
        description: "Prometheus is receiving out-of-order samples ({{ $value | humanize }}/s). This usually indicates clock skew or a buggy exporter."
    - alert: PrometheusTsdbOutOfBoundSamples
      expr: rate(prometheus_tsdb_out_of_bound_samples_total[5m]) > 0
      labels:
        severity: warning
      annotations:
        summary: "Prometheus TSDB out-of-bound samples (instance {{ $labels.instance }})"
        description: "Prometheus is rejecting samples with timestamps too old or too far in the future ({{ $value | humanize }}/s). Check target clocks."
    - alert: PrometheusTargetLimitExceeded
      # prometheus_target_scrape_pool_exceeded_target_limit_total carries no
      # labels, so the annotation cannot name the offending pool.
      expr: increase(prometheus_target_scrape_pool_exceeded_target_limit_total[5m]) > 0
      labels:
        severity: warning
      annotations:
        summary: "Prometheus scrape pool target limit exceeded (instance {{ $labels.instance }})"
        description: "Prometheus is silently dropping targets in job {{ $labels.instance }} because the target limit was exceeded."
    - alert: PrometheusScrapedBodySizeExceeded
      expr: rate(prometheus_target_scrapes_exceeded_body_size_limit_total[5m]) > 0
      labels:
        severity: warning
      annotations:
        summary: "Prometheus scrape body size limit exceeded (instance {{ $labels.instance }})"
        description: "Prometheus is truncating scrape responses that exceed the body size limit ({{ $value | humanize }} scrapes/s). The exporter may be too verbose."
    - alert: PrometheusSDFailedConfigs
      expr: prometheus_sd_failed_configs > 0
      for: 1m
      labels:
        severity: warning
      annotations:
        summary: "Prometheus service discovery has failed configs (instance {{ $labels.instance }})"
        description: "{{ $value }} service discovery config(s) are failing (name={{ $labels.name }}). Targets from these configs will not be scraped."


    - alert: PrometheusNotificationsDropped
      # The failure that hides every other failure: alerts discarded on the way
      # to Alertmanager. Nothing else here notices -- the rules still evaluate,
      # the targets are still up, and no page arrives.
      #
      # Aggregating `without (alertmanager)` rather than `by (instance)` is
      # deliberate. The metric is a plain Counter up to at least 3.0 and a
      # CounterVec labelled by alertmanager in 3.13; both versions run here.
      # `without` is a no-op on the old shape and collapses the queues on the
      # new one, so this is one alert per Prometheus either way.
      # Scaled to alerts/minute purely so the annotation reads as a number a
      # human can act on: humanize renders 0.05/s as "50m", which at 3am is
      # worse than useless. The > 0 threshold is unaffected by the scaling.
      expr: sum without (alertmanager) (rate(prometheus_notifications_dropped_total[5m])) * 60 > 0
      for: 5m
      labels:
        severity: critical
      annotations:
        summary: "Prometheus is dropping alert notifications (instance {{ $labels.instance }})"
        description: "{{ $value | humanize }} alerts/min are being dropped on the way to Alertmanager by {{ $labels.instance }}. Alerts are firing and nobody is being told."
