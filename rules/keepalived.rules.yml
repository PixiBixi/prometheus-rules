- name: keepalived.rules
  rules:
    - alert: KeepalivedDown
      expr: keepalived_up == 0
      for: 2m
      labels:
        severity: critical
      annotations:
        summary: "Keepalived down on {{ $labels.instance }}"
        description: "Keepalived is unreachable on {{ $labels.instance }}. VIP failover may be broken."

    - alert: KeepalivedSplitBrain
      # MASTER is 2, not 1. cafebazaar/keepalived-exporter exports the raw VRRP
      # state int, and its enum is VRRPStates = {INIT, BACKUP, MASTER, FAULT},
      # so == 1 counted BACKUP nodes: true on any healthy cluster with two or
      # more standbys, and never true for an actual two-MASTER split brain.
      expr: count by (vrid) (keepalived_vrrp_state == 2) > 1
      for: 1m
      labels:
        severity: critical
      annotations:
        summary: "Keepalived split-brain detected on VRID {{ $labels.vrid }}"
        description: "Multiple instances are in MASTER state for VRID {{ $labels.vrid }}. This causes IP conflicts on the network."

    - alert: KeepalivedNoMaster
      # Two bugs here. Besides == 1 selecting BACKUP rather than MASTER,
      # `count by (vrid) (...) == 0` can never be true: when nothing matches,
      # count by() yields no group at all rather than a zero, so the comparison
      # has nothing to test. Select the VRIDs that exist and subtract those that
      # do have a master.
      expr: |
        count by (vrid) (keepalived_vrrp_state)
        unless
        count by (vrid) (keepalived_vrrp_state == 2)
      for: 2m
      labels:
        severity: critical
      annotations:
        summary: "No Keepalived master for VRID {{ $labels.vrid }}"
        description: "No instance is in MASTER state for VRID {{ $labels.vrid }}. The VIP is unreachable."

    - alert: KeepalivedFlapping
      expr: increase(keepalived_become_master_total[5m]) > 3
      for: 0m
      labels:
        severity: warning
      annotations:
        summary: "Keepalived VRRP flapping on {{ $labels.instance }}"
        description: "{{ $labels.instance }} has transitioned to MASTER {{ $value }} times in 5 minutes for VRID {{ $labels.vrid }} — network or configuration instability."

    - alert: KeepalivedAuthFailure
      expr: increase(keepalived_authentication_failure_total[5m]) > 0
      for: 0m
      labels:
        severity: warning
      annotations:
        summary: "Keepalived authentication failures on {{ $labels.instance }}"
        description: "{{ $labels.instance }} is receiving VRRP packets with authentication failures — possible misconfiguration or unexpected peer on the network."

    - alert: KeepalivedAdIntervalErrors
      expr: |
        (
          increase(keepalived_advertisements_interval_errors_total[5m])
          /
          increase(keepalived_advertisements_received_total[5m])
        ) > 0.05
        and
        increase(keepalived_advertisements_received_total[5m]) > 5
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Keepalived advertisement interval errors on {{ $labels.instance }}"
        description: "More than 5% of received VRRP advertisements have interval errors on {{ $labels.instance }} vrid={{ $labels.vrid }} ({{ $value | humanizePercentage }}) — risk of spurious master election."

    - alert: KeepalivedAuthenticationInvalid
      expr: increase(keepalived_authentication_invalid_total[5m]) > 0
      for: 0m
      labels:
        severity: warning
      annotations:
        summary: "Keepalived authentication invalid on {{ $labels.instance }}"
        description: "VRRP authentication failures on {{ $labels.instance }} vrid={{ $labels.vrid }} — likely misconfiguration or rogue peer."

    - alert: KeepalivedTTLErrors
      expr: |
        (
          increase(keepalived_ip_ttl_errors_total[5m])
          /
          increase(keepalived_advertisements_received_total[5m])
        ) > 0.02
        and
        increase(keepalived_advertisements_received_total[5m]) > 5
      for: 0m
      labels:
        severity: warning
      annotations:
        summary: "Keepalived TTL errors on {{ $labels.instance }}"
        description: "More than 2% of received VRRP advertisements have TTL errors on {{ $labels.instance }} vrid={{ $labels.vrid }} ({{ $value | humanizePercentage }}) — possible routing misconfiguration or rogue peer."

    - alert: KeepalivedPacketErrors
      expr: increase(keepalived_packet_length_errors_total[5m]) > 0
      for: 0m
      labels:
        severity: warning
      annotations:
        summary: "Keepalived packet length errors on {{ $labels.instance }}"
        description: "VRRP packet length errors on {{ $labels.instance }} — possible network corruption or incompatible version."
